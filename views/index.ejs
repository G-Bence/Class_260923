<%- include('partials/header') %>
    <br>
    <h2>Total Expenses: <%= totalAmount %> Ft</h2>
    <br>
    <h3>Expenses:</h3>
    <ul>
        <% allExpenses.forEach(expense => { %>
            <li><%= expense.name %>: <%= expense.amount %> (Category: <%= expense.category %>)</li>
        <% }); %>
    </ul>
<%- include('partials/footer') %>